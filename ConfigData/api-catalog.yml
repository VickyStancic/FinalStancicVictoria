server:
  port: ${PORT:8081}
spring:
  application:
    name: api-catalog
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/api-catalog
    username: root
    password: yocostancic2
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true