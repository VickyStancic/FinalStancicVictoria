server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: api-movie
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/api-movie
    username: root
    password: yocostancic2
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true